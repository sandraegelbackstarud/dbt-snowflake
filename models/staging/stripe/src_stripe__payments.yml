version: 2

sources:
  - name: stripe  # This should be a dictionary
    database: raw  # This is the database name
    schema: stripe  # This is the schema name in Snowflake
    tables:  # This is where tables should be listed as a dictionary
      - name: payment  # This is the table name, make sure it matches exactly with Snowflake
        loaded_at_field: _batched_at
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}
